from typing import *


def sum_of_list(x: List[int]):
    print(sum(x))


sum_of_list([1, 2, 3, 4])
# Here if we will hover on the function sum_of_list then we will be able to see that it will take list as argument and in which all values have data type as integer.
