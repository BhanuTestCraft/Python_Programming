def sum_of_list(x):
    print(sum(x))


sum_of_list([1, 2, 3, 4])
# Here if we hover on the function defined sum_of_list then we will be able to see x-->Any so here we need to use some sort of library.
